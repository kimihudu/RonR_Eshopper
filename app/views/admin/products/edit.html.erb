<%= simple_form_for(@prod,:url => admin_product_path,:method => :PATCH,
        html: {class: 'form-horizontal',:mulitipart => true},:defaults => {:input_html => {:class => 'form-control'}}) do|f|%>
<div class="row">


<div class="col-md-6" style="margin-top:25px">
    <div class="content-box-large ">



            <!-- {!! Form::open(['url' => 'editProduct', 'method' => 'post']) !!} -->
            <div class="panel-heading col-md-8">
                <div class="panel-title">Edit Product

                </div>
            </div>

            <!-- <?php $cats=A pp\Categorys::all(); ?> @foreach($Products as $product) -->

            <div class="panel-body">
                <div class="col-md-8">

                    <!-- <br> Category:
                <select name="cat_id" class="form-control">
                     @foreach($cats as $cat)
                    <option value="{{$cat->name}}" </option>
                        @endforeach
                </select> -->

                        <br/>
                        <%= f.input :id, :label => 'Product ID:' %>

                        <br/>
                        <%= f.input :name, :label => 'Product Name' %>

                        <br/>

                        <%= f.input :brand_name, :label => 'Brand Name:' %>

                        <br/>

                        <%= f.simple_fields_for :cat_name do |ff|%>
                             <%= ff.input :name, :label => 'Category' %>

                            <br/>
                            <%= ff.input :sub, :label => 'Sub-Category' %>
                        <% end %>

                        <br/>
                        <%= f.input :price, :label => 'Unit Price:' %>

                        <br/>
                        <%= f.input :unit_qTy, :label => 'Unit In Stock:' %>

                        <br/>
                         <!-- <%= f.input :size,  :label => 'Size:' %>  -->

                        <br/>
                         <!-- <%= f.input :history, :label => 'Details:' %>  -->

                        <br/> New Arrival:
                        <p class="pull-right">
                            <input type="checkbox" name="new_arrival" value="1" />
                            <input type="hidden" name="_token" value="{{ csrf_token() }}">

                        <br/>

                        <%= f.button :submit, :label => 'Update product',
                        :class=> 'btn btn-success pull-right', style: "margin-top:10px" %>
                </div>

            </div>

    </div>

</div>

<div class="col-md-4" style="margin-top:25px; margin-left:-4px">
    <!-- <?php $pId=$ Products[0]->id; $prots = DB::table('products_properties') ->where('pro_id', $pId)->get(); if(count($prots)==0) { } else {?> -->
    <div class="panel-heading" style="height:120px; background-color:white">
        <div class="panel-title">
            Update Proprities


            <!-- <input type="text" name="spl_price" class="form-control" border="none" value=""> -->
            <a href="" class="btn btn-info pull-right" style="margin:-6px; color:#fff">Add more</a>
        </div>

        <div class="col-sm-12" style="margin-top:100px; ">
            <div class="content-box-medium " align="center">
                <div class="panel-heading">
                    <div class="panel-title">
                        Update Product Image
                    </div>
                </div>
                   <br/>

                <img src="<%= @prod.img%>" alt="" class="img-rounded img-current" accept="images/*" width="150px" height="150px" />
                <br>
                <br>
                <p>
                    <%= f.file_field :img, :id => 'prod_img', style: 'display: none', class: 'btn btn-info'%>
                    <input type="button" style="margin-left:45px" value="Browse..." class="btn btn-info btn-browser" />
                    <script>
                        $('.btn-browser').on('click',function(e){
                            $('#prod_img').click();
                            $('#prod_img').show();
                            $('.img-current').hide();
                            $('.btn-browser').hide();

                        })
                    </script>
                </p>
            </div>
        </div>
    </div>
    <div class="col-md-2">
    </div>

</div>
</div>
  <% end %>

 <!-- <br/>
                <input type="hidden" name="id" class="form-control" value=""> Name:
                <input type="text" name="pro_name" class="form-control" value="">
                <br/> Price:
                 <input type="text" name="pro_size" class="form-control" value="">
                <br/> Size:
                <input type="text" name="pro_price" class="form-control" value="">
                <br/> Code:
                 <input type="text" name="pro_color" class="form-control" value="">
                <br/> Color:
                <input type="text" name="pro_code" class="form-control" value="">
                <br/> Sale price:
                <input type="text" name="spl_price" class="form-control" value="">
                <br/> Details:
                <textarea name="pro_info" class="form-control" rows="2"></textarea> -->
<!--<div class="content-box-large">

    <table class="table table-responsive">
        <tr>
            <td>Size</td>
            <td>Color</td>
            <td>price</td>
            <td>Update</td>
        </tr>
        @foreach($prots as $prot) {!! Form::open(['url' => '/editProperty', 'method' => 'post']) !!}
        <tr>
            <input type="hidden" name="pro_id" value="{{$prot->pro_id}}" size="6" />
            <!-- products_properties pro_id
            <input type="hidden" name="id" value="{{$prot->id}}" size="6" />
            <!--// products_properties id -->
<!-- <td>
                <input type="text" name="size" value="{{$prot->size}}" size="6" />
            </td>
            <td>
                <input type="text" name="color" value="{{$prot->color}}" size="15" />
            </td>
            <td>
                <input type="text" name="p_price" value="{{$prot->p_price}}" size="6" />
            </td>
            <td colspan="3" align="right">
                <input type="submit" class="btn btn-success" value="Save" style="margin:-6px; color:#fff">
            </td>
        </tr>
        <!-- {!! Form::close() !!} @endforeach

    </table>-->
<!-- </div>

<hr>

<div class="content-box-large " align="center">
    <div class="panel-heading">
        <div class="panel-title">
            Update Product Image
        </div>
    </div>

    <img src="{{asset($product->img)}}" alt="" class="img-rounded" width="150px" height="150px" />
    <br>
    <br>
    <p><a href="{{url('/EditImage')}}/{{$product->id}}" class="btn btn-info">Change Image</a>
    </p>
</div>
</div>-->